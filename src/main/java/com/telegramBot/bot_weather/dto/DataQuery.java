package com.telegramBot.bot_weather.dto;

public enum DataQuery {
    menu,
    city_save, city_delete, city_all, city_list, city_verification_add, city_verification_delete,
    weather, weather_current, weather_today, weather_forecast
}
